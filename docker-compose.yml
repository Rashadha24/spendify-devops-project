version: '3.8'
services:
    app:
    build: 
    ports:
        -"5173:5173"
    environment:
            VITE_API_URL=http://localhost:8000
    Volumes:
        - .:/app
        - /app/node_modules
